---
interface SkillCategory {
	name: string;
	keywords: string[];
}

interface Props {
	skills: SkillCategory[];
}

const { skills } = Astro.props;

const techCategories = ["Lenguajes de Programación", "Frameworks", "Otras Áreas"];
const techSkills = skills.filter(s => techCategories.includes(s.name));
---

<section>
	<h3 class="text-xl font-bold uppercase tracking-widest text-[#9eb9ff] mb-3 font-heading">Tecnologías</h3>
	
	<div class="space-y-2.5">
		{techSkills.map((category) => (
			<div>
				<h4 class="text-xs font-bold uppercase text-white/60 mb-1.5 font-heading">{category.name}</h4>
				<p class="text-white/80 text-sm leading-relaxed font-light">
					{category.keywords.join(' • ')}
				</p>
			</div>
		))}
	</div>
</section>
